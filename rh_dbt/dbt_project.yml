name: 'rh_dbt'
version: '1.0.0'
config-version: 2

profile: 'rh_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  rh_dbt:
    staging:
      +schema: dbt_staging
      +materialized: view
    marts:
      +schema: gold
      +materialized: table